"""CertHub Cadence connector — sync, evidence gate, and Release Record push.

Hand-written package for the public Cadence showcase. Generated OpenAPI clients
live under ``api/clients/`` (public endpoints only); validated JSON models under
``api/api_models/``. Prefer the thin wrappers in ``api.client``.
"""
