"""Generated Pydantic models for Tech Doc and Records JSON.

Built by ``make generate-api`` (``datamodel-code-generator``) from the same
public-only OpenAPI specs as ``api/clients/``. Used at the API boundary for
validation; HTTP wire types stay in the attrs client trees.
"""
